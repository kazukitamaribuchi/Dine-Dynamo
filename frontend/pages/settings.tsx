import SettingsView from "@/components/views/SettingsView";
import type { NextPage } from "next";

const Index: NextPage = (props) => {
  return <SettingsView props={props} />;
};

export default Index;
