import type { NextPage } from "next";
import FacebookView from "@/components/views/FacebookView";

const Index: NextPage = (props) => {
  return <FacebookView props={props} />;
};

export default Index;
