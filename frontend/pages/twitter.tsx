import type { NextPage } from "next";
import TwitterView from "@/components/views/TwitterView";

const Index: NextPage = (props) => {
  return <TwitterView props={props} />;
};

export default Index;
