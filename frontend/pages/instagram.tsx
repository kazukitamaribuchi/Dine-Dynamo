import type { NextPage } from "next";
import InstagramView from "@/components/views/InstagramView";

const Index: NextPage = (props) => {
  return <InstagramView props={props} />;
};

export default Index;
