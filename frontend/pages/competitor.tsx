import CompetitorView from "@/components/views/CompetitorView";
import type { NextPage } from "next";

const Index: NextPage = (props) => {
  return <CompetitorView props={props} />;
};

export default Index;
